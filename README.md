# Anthrosevka Mono

## Build

```bash
git clone https://github.com/be5invis/Iosevka.git --depth 1
cd Iosevka
npm install
```

Put `private-build-plans.toml` in the root of the project, then run

```bash
npm run build -- ttf::AnthrosevkaMono
```
